import { DBQuiz } from "types/quiz"

export default function MainQuizLayout({ quiz, tab }: LayoutProps) {
  return (
    <div>
      <h1></h1>
    </div>
  )
}

type LayoutProps = {
  quiz: DBQuiz
  tab: number
}
